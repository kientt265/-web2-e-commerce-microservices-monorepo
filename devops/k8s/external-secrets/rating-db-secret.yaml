apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: rating-db-secret
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: aws-secret-store
    kind: ClusterSecretStore
  target:
    name: rating-db-secret
    template:
      engineVersion: v2
      data:
        # URL encode password để tránh lỗi ký tự đặc biệt
        DATABASE_URL: |
          postgresql://postgres:{{ .password }}@{{ .endpoint }}:5432/rating_db?sslmode=require
        PASSWORD: "{{ .password }}"
        ENDPOINT: "{{ .endpoint }}"
  data:
  - secretKey: password
    remoteRef:
      key: rds-password
      property: password
  - secretKey: endpoint
    remoteRef:
      key: rds-password
      property: endpoint
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: aws-secret-store
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-southeast-1
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets
