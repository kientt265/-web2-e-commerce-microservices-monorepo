apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: rating-db-secret
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: aws-secret-store
    kind: ClusterSecretStore
  target:
    name: rating-db-secret
    creationPolicy: Owner
  data:
  - secretKey: DATABASE_URL
    remoteRef:
      key: rds-password-rating-db